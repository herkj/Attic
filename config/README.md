# config/

This folder is gitignored. It contains user-specific settings that survive `git pull`.

## Contents

- `taxonomy/custom.yaml` - Your domain-specific taxonomy, generated by `/setup-attic` from your product websites. Edit it anytime.

Changes here will never be overwritten by updates to Attic.
